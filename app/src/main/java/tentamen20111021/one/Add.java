package tentamen20111021.one;

public class Add {
    private int address1, address2, address3;

    public void execute(Word[] memory) {
        memory[address3].add(memory[address1], memory[address2]);
    }
}
